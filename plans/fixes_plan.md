# План исправлений для второго этапа конкурса

## Анализ текущего состояния

### Что уже реализовано:
1. ✅ Роли (ADMIN, CHIPPER, USER) - в schema.prisma
2. ✅ Автоматическое создание аккаунтов при первом запуске - в seed.ts
3. ✅ CRUD для зон (areas) - в areaService.ts, areaController.ts, areaRoutes.ts
4. ✅ Аналитика по зонам - в areaRepository.ts (метод getAreaAnalytics)
5. ✅ JWT аутентификация - в middleware/auth.ts
6. ✅ Валидация - в validation/*.ts
7. ✅ Обработка ошибок - в errors/httpErrors.ts

### Проблемы, которые требуют исправления:

## 1. docker-compose.yml - Не соответствует требованиям документации

### Текущие проблемы:
- Название БД: `animal-chipization` ✅ (уже исправлено)
- Пользователь: `user` ✅ (уже исправлено)
- Пароль: `password` ✅ (уже исправлено)
- Volume: `/var/lib/postgresql/data/` ✅ (уже исправлено)
- URL подключения: `postgresql://user:password@database:5432/animal-chipization?schema=animals_area` ✅ (уже исправлено)

### Статус: ✅ Исправлено

## 2. Аналитика по зонам - Логика подсчета уникальных входов/выходов

### Требование из документации:
> "Одно и то же животное может несколько раз посетить и покинуть зону в указанный период. Для каждого животного учитывается только один вход в зону и один выход из нее."

### Текущая проблема:
Текущая реализация в `areaRepository.ts` считает все посещения, а не уникальные входы/выходы.

### Требуемая логика:
1. **totalQuantityAnimals** - количество уникальных животных, которые находились в зоне в указанный период
2. **totalAnimalsArrived** - количество уникальных животных, которые ВОШЛИ в зону в указанный период (первое посещение зоны в периоде)
3. **totalAnimalsGone** - количество уникальных животных, которые ВЫШЛИ из зоны в указанный период (последнее посещение зоны в периоде)
4. **animalsAnalytics** - аналитика по типам животных с теми же правилами

### Алгоритм:
Для каждого животного:
1. Найти все посещения зоны в указанном периоде
2. Определить первое посещение (минимальная дата) - это "вход"
3. Определить последнее посещение (максимальная дата) - это "выход"
4. Если первое посещение в периоде - считать как "прибытие"
5. Если последнее посещение в периоде - считать как "уход"

### Статус: ❌ Требует исправления

## 3. Валидация аналитики

### Текущее состояние:
В `areaSchemas.ts` используется `isoDateSchema` с regex `/^\d{4}-\d{2}-\d{2}$/`, что соответствует формату "yyyy-MM-dd".

### Статус: ✅ Корректно

## 4. Проверка соответствия API спецификации

### GET /areas/{areaId}/analytics
- ✅ Метод GET
- ✅ Параметры пути: areaId
- ✅ Параметры запроса: startDate, endDate
- ✅ Формат дат: yyyy-MM-dd
- ✅ Валидация: startDate < endDate
- ✅ Аутентификация: требуется
- ✅ Роли: не указаны (доступно всем аутентифицированным)

### Статус: ✅ Корректно

## 5. Проверка других endpoints

### GET /areas/{areaId}
- ✅ Метод GET
- ✅ Параметры пути: areaId
- ✅ Аутентификация: требуется
- ✅ Роли: не указаны (доступно всем аутентифицированным)

### POST /areas
- ✅ Метод POST
- ✅ Тело запроса: name, areaPoints
- ✅ Аутентификация: требуется
- ✅ Роли: ADMIN

### PUT /areas/{areaId}
- ✅ Метод PUT
- ✅ Параметры пути: areaId
- ✅ Тело запроса: name, areaPoints
- ✅ Аутентификация: требуется
- ✅ Роли: ADMIN

### DELETE /areas/{areaId}
- ✅ Метод DELETE
- ✅ Параметры пути: areaId
- ✅ Аутентификация: требуется
- ✅ Роли: ADMIN

### Статус: ✅ Все endpoints корректны

## Порядок выполнения исправлений

1. **Исправить логику аналитики в areaRepository.ts** ❌
   - Переписать метод getAreaAnalytics
   - Реализовать подсчет уникальных входов/выходов
   - Реализовать аналитику по типам животных

2. **Протестировать изменения**
   - Запустить docker-compose up
   - Проверить работу endpoints
   - Проверить автотесты

## Примечания

- База данных должна быть PostgreSQL latest
- Все пароли и хеширование паролей должны использовать bcrypt
- JWT токены должны использоваться для аутентификации
- Роли: ADMIN, CHIPPER, USER
- При регистрации роль по умолчанию: USER
